# GEO 경쟁 제품 리서치 로그

목적: 경쟁/유사 제품 자료를 계속 모아두고, 다 모인 뒤 한 번에 정리해서
기획 → 작업 순서로 진행한다. 지금 단계에서는 **분석만 쌓아두고 코드/앱
콘텐츠(help.ts 등)에는 반영하지 않는다.**

각 항목: 원문 출처 요약 → 우리 제품과 비교했을 때 눈에 띄는 점 → 반영
여부 판단(반영 검토/이미 있음/방향 다름)은 전체 리서치가 끝난 뒤 마지막에
한 번에 정리한다. 지금은 "관찰"만 기록.

---

## 2026-09-23 — VIVI (Content Assistant + Insight/Action)

### 원문 요약
- **VIVI Content Assistant**: 크롬 사이드패널 확장 프로그램. 지금 보고
  있는 페이지를 9개 모듈(AI 크롤러 접근성, 구조화 데이터, 메타데이터,
  인용 신뢰도 등)로 진단, 100점 환산 점수 제공. CMS 편집 화면에서는
  JSON-LD·메타데이터·FAQ를 바로 삽입 가능한 형태로 생성. 자체 포함 HTML
  리포트로 공유, 최근 진단 40개 이력 보관.
- **VIVI Insight**: 국내외 AI 엔진 6개(Naver AI Briefing, Google AI
  Overview, AI Mode, Gemini, Perplexity, ChatGPT — Claude/Bing/Yahoo는
  지원 예정)에서 동일 질문의 답변을 수집. Mention Rate·Citation
  Rate·Share of Voice·Visibility Funnel(단계별 이탈) 4개 지표로 측정.
  플랜별로 엔진/프롬프트 수 다르고, 브랜드 아래 프로젝트 단위로 구분.
- **VIVI Action**: Insight가 찾은 약점에 대응하는 콘텐츠 생성/개선 실행.
  취약 토픽 → 콘텐츠 생성, 통이미지 상세페이지 → AI가 읽는 텍스트(PDP
  변환)로 전환. 실행 후 같은 지표로 재측정.
- 사용 흐름: Setup(최초 1회) → Metrics → Topics → Topic Research →
  Pages, 이후 매주 같은 순서로 반복.

### 관찰 (우리 제품과 비교)
- **없는 개념**: 구조화 데이터(JSON-LD) 진단/생성이 별도 기회 유형으로
  없음. 언급→인용 단계별 퍼널 뷰가 없음(우리는 독립된 숫자만). 수집
  엔진이 네이버 AI검색·구글 AI Overview 2개뿐(Perplexity/ChatGPT/Gemini
  없음).
- **개념은 있는데 형태가 다름**: Share of Voice ≈ 우리 브랜드 가시성의
  마켓 트래킹 비교. PDP 변환 ≈ 우리 멀티미디어 가시성 보강. 진단 이력
  40개 ≈ 우리 토픽 상세의 "수집 로그별 변화".
- **배포 형태 자체가 다른 것**: 크롬 익스텐션형 실시간 단일 페이지
  진단(CMS 편집 화면에 바로 삽입) — 우리는 사이트맵 전체 크롤 기반
  대시보드. 별도 프로덕트 라인에 가까움.

---

## 2026-09-23 — VIVI 공식 문서 (Getting Started 4편)

### 원문 요약
- **제품 개요**: AI 답변은 검색 결과와 달리 순위가 없고 질문 하나에
  답변 하나 — 언급 안 되면 그 화면엔 아예 존재하지 않는다는 게 측정
  필요성의 근거. 측정(Insight) → 실행(Action) → 재측정(Insight)이
  한 번으로 안 끝나고 반복되는 루프. 개선 반영 표시를 한 시점의
  인용률을 **기준선(baseline)**으로 기록해두고, 그 대비 상승폭을
  산출한다.
- **계정 구조**: Organization(계약 단위) > Brand(측정 대상 브랜드,
  이름 변형+공식 도메인 등록) > Project(실제 측정 단위 — 국가·엔진·
  자동수집 설정, 국가 하나만 가짐이라 시장별로 Project를 나눔). 지표는
  Project 단위 집계, Project끼리 합산 안 함. 탭 구성: Brand / **Brand
  Voice**(생성물 어조·표현 규칙, PDP Convert·Page Optimizer·Localizer에
  공통 적용) / Project / Competitor / Integrations / Reports / Plan &
  usage / Access. 질문 추가/삭제는 별도 화면(Topics의 Modify Prompts)에서.
- **측정 방식**: 단위는 Category(선택, Topic 묶음) > Topic(검색 키워드
  1개 대응, 측정 여부 결정 단위) > Prompt(실제 전송 문장). 파이프라인:
  ①승인된 Prompt 세트 ②AI 엔진 질의(Prompt×엔진 전 조합) ③답변 원문+
  출처 URL 저장 ④언급/인용 판정 ⑤날짜·엔진·Topic별 집계. 수집은 주
  1회, 한국시간 자정 직후 시작·오전 6시경 집계, 요일은 온보딩일 기준
  Organization마다 다름. **답변 변동성**을 정면으로 문서화 — 같은 질문도
  매번 다르게 답하는 게 정상이라 하루 숫자가 아닌 추이로 판단하라고
  안내. 고객이 "내가 직접 물어보니 나오던데 왜 0%냐"는 문의에 대한
  공식 답변까지 문서화(질문 문구·계정·지역·시점이 다 다르기 때문).
  판단 기준 3가지: ①자사 지표 추이(가장 신뢰) ②Share of Voice(경쟁사
  대비 상대 위치) ③**AI Existence**(그 주제로 AI 답변 자체가 생성되는
  비율 — 이게 낮으면 Mention이 낮은 원인이 콘텐츠가 아니라 "애초에 AI가
  답을 안 만듦"). 피해야 할 해석도 명시: 인과 단정 금지(모델 자체가
  갱신되면 아무것도 안 해도 숫자가 움직임), SoV를 실제 시장점유율로
  착각 금지(등록한 Competitor 안에서의 비중일 뿐).
- **AI 엔진별 특성**: 최대 7개 엔진 — ChatGPT(대화형), Google(검색 내
  AI Overview), Google AI Mode(검색과 분리된 대화형), NAVER(AI
  브리핑, 한국어만, 브리핑 내 쇼핑카드는 인용이 아니라 "제품 노출"로
  별도 표시), NAVER AI Tab(대화형, rollout 켜진 계정만), Gemini(대화형),
  Perplexity(출처 표기 전제). 엔진 간 절대 비교를 하지 말라고 명시 —
  엔진마다 출처를 다는 습성 자체가 달라서(Perplexity는 거의 항상,
  어떤 엔진은 드물게) 인용률 차이가 성과 차이가 아니라 엔진 성격 차이일
  수 있음. Google 계열 인용은 검색 결과의 중간 리다이렉트 링크가 아니라
  실제 도착 페이지 주소로 정리해서 저장. Google AI Overview·NAVER AI
  브리핑은 검색 결과 안에 뜨는 블록이라 질문에 따라 아예 안 뜰 수 있음
  — 그래서 이 두 엔진은 AI Existence가 구조적으로 낮게 나오고, Mention
  Rate를 읽을 때 반드시 AI Existence를 같이 보라고 안내.

### 관찰 (우리 제품과 비교)
- **AI Existence 개념이 우리한텐 없음**: 우리는 수집 실행의 성공/실패
  (봇 차단 등)만 구분하지, "그 프롬프트에 대해 AI가 애초에 답변 블록을
  만들었는지"를 별도 지표로 안 본다. 특히 우리가 이미 다루는 Google AI
  Overview는 문서에 따르면 구조적으로 안 뜨는 경우가 많은 엔진이라,
  우리 "미노출" 판정이 "콘텐츠 문제"인지 "애초에 AI Overview 자체가 그
  질문엔 안 뜨는 것"인지 구분이 안 될 수 있음 — 지표 해석에 영향.
- **수집 주기가 근본적으로 다름**: VIVI는 주 1회 자동 배치, 우리는
  사람이 로컬에서 수동으로 트리거. 이건 이미 로드맵(db-collection-
  pipeline)에 있는 "AI 수집 파이프라인의 서버 실행" 항목과 직결.
- **계정 구조(Org>Brand>Project)가 우리 "다중 조직" 로드맵과 겹침**:
  특히 "같은 브랜드, 다른 시장은 Project 추가"라는 규칙은 우리가 지금
  마켓을 브랜드 하위 속성(markets 배열)으로만 다루는 것과 다른 모델 —
  나중에 다중 조직/다중 시장 설계할 때 참고할 만한 대안 구조.
- **Brand Voice(어조·표현 규칙)를 여러 생성 기능에 공통 적용하는 개념이
  없음**: 우리는 LLM 브릿지 기능(가이드 등록 등)마다 어조 설정이 없음.
  LLM API 연동 이후 콘텐츠 생성 기능이 늘어나면 브랜드 톤 일관성 문제가
  생길 수 있어 참고할 만함.
- **Localizer(다른 시장 언어로 페이지 재작성) 개념이 없음**: 우리는
  마켓별 콘텐츠 현지화 기능 자체가 없음.
- **"기준선(baseline) 기록 후 상승폭 산출" 메커니즘**: 우리도 "수정
  완료 확인" 재크롤이 있지만, 그건 크롤 점수(가독성 등) 재검증이지
  실제 언급/인용률의 전/후 비교가 아님. VIVI는 반영 시점의 인용률
  자체를 기준선으로 못박고 그 이후 실측 상승폭을 보여줌 — 더 설득력
  있는 "효과 증명" 방식일 수 있음.
- **해석 가이드 문서화 자체가 좋은 패턴**: "답변 변동성은 정상", "직접
  물어본 결과와 다를 수 있는 이유", "인과 단정 금지", "SoV는 시장
  점유율이 아님" 같은 안내를 공식 문서에 미리 박아두는 방식은, 우리
  도움말에도 비슷한 "숫자를 어떻게 읽어야 하는지" 섹션이 없어서 참고할
  가치가 있음(특히 우리도 감성/언급 수가 실행마다 흔들릴 수 있는 구조).

---

## 2026-09-23 — VIVI 온보딩 플로우 (스크린샷 9장 + 문서 3편)

### 원문 요약
- **온보딩 입력(한 화면)**: Your brand(브랜드+Competitor) → Your
  market(시장+카테고리+Prompt style) → Measurement plan(AI 엔진 선택)
  → Start monitoring 버튼. **Detect brand**: 홈페이지 URL만 넣으면
  브랜드명·이름 변형·Competitor 후보·카테고리를 자동으로 읽어 채움.
  Competitor는 "이름+도메인" 쌍 필수(최소 1~최대 15) — 도메인이 있어야
  인용 판정 가능, 이름만으로는 언급만 잡힘. Competitor가 하나도 없으면
  자사 언급 시 Share of Voice가 100%가 돼 비교 의미 상실(경고 문구로
  명시). 카테고리는 자동 발굴 가능(비워두면), 카테고리마다 "측정 슬롯"
  보장. **Prompt style** 4종(Consumer & retail / High-consideration /
  Services & places / Deep-tech & expertise)을 고르면 그 업종 구매자가
  실제로 AI에게 묻는 말투로 질문이 생성되고, 스타일별로 "성과가 언급으로
  나타나는지 인용으로 나타나는지"까지 미리 안내(예: Deep-tech는 인용
  중심이 정상).
- **2단계 확인 절차**: Start monitoring 직후에는 아무것도 측정 안 됨.
  ①Topic 후보 발굴 → 검토/수정 → 확정(질문 생성 시작) ②질문(Prompt)
  생성 → 검토/수정/삭제 → 최종 확정해야 첫 수집 시작. 최소 규모 미달 시
  경고("Topic 5개(질문 약 50개) 이상이어야 측정 가능"). **Simulate**
  기능: 확정 전에 그 Topic이 만들 질문 전체 미리보기 + AI가 어떤 형태로
  답할지 예측 텍스트까지 보여줌(실제와 다를 수 있음을 명시). 질문마다
  Intent 태그(Needs/Research/Compare/Decide/AI Recommend) 부여, 그
  자리에서 수정/삭제 가능. "Generate with AI"로 질문 추가 생성(개수
  조절 가능). 세팅은 창을 닫아도 백그라운드로 계속 진행.
- **첫 데이터까지**: 진행 6단계를 화면에 그대로 노출 — Mapping your
  market → Topic review → Writing prompts → Question review → First
  crawl → First numbers. **"0%"와 "하이픈(-)"을 의도적으로 구분** —
  0%는 "측정했지만 언급 없음", 하이픈은 "아직 측정 자체를 안 함". 숫자가
  없는 게 오류가 아니라 정상 대기 상태임을 못박음.
- **측정 대상 관리(변경 정책)**: Brand 이름/도메인, Competitor,
  Prompt, Topic 4가지가 변경 가능 대상. **변경은 다음 수집부터만
  반영, 과거 데이터는 절대 재계산 안 함**(오늘 Competitor 추가해도
  지난 수집엔 소급 안 됨 — 명시적 정책). 이름 변형을 너무 짧은 일반
  명사로 등록하면 무관한 문장까지 언급으로 잡히는 리스크도 경고.
  **Modify Prompts 화면**: 질문 추가/삭제/수정을 "스테이징"만 해두고
  (Pending changes/New/Edited/Removed/Paused 탭으로 상태 구분) "Apply
  N change(s)" 버튼을 눌러야 실제 반영 — 누르기 전엔 아무것도 안
  바뀜. Prompt 세트가 바뀌면 지표 추이 그래프에 "Prompt changes"
  세로선을 표시해 그 시점 전후 데이터를 같은 기준으로 비교하면 안
  된다는 걸 시각적으로 알려줌. Topic 추가는 플랜 한도(Plan headroom)에
  걸리면 기존 Topic과 "Swap"하거나 플랜 업그레이드를 유도.

### 관찰 (우리 제품과 비교)
- **브랜드 자동 감지(Detect brand)가 없음**: 우리는 브랜드 등록 시
  이름/URL/별칭/카테고리를 전부 수동 입력. 홈페이지 URL만으로 초안을
  채워주는 온보딩은 신규 브랜드 추가 마찰을 크게 줄이는 패턴.
- **Prompt style(업종별 질문 톤 프리셋)이 없음**: 우리는 프롬프트를
  전부 개별 작성/AI 생성하지, "이 업종은 이런 식으로 물어본다"는
  프리셋 자체가 없음. 카테고리 등록 시 이런 프리셋을 고르게 하면
  초기 프롬프트 세트 품질이 올라갈 수 있음.
- **최소 측정 규모 가드레일이 없음**: 우리는 토픽/프롬프트가 1개뿐이어도
  경고 없이 그냥 보여줌. "이 정도는 있어야 통계적으로 의미 있다"는
  최소 기준 안내가 없음 — 특히 실행 수가 적은 브랜드에서 유용할 수 있음.
- **Simulate(확정 전 미리보기+AI 답변 예측)가 없음**: 우리는 프롬프트
  추적을 누르면 바로 라이브러리에 들어감. "이 질문을 추적하면 대략
  이런 답이 나올 것"이라는 예측 미리보기는 없음 — LLM API 연동 후 검토
  가치 있는 패턴.
- **"0% vs 하이픈" 구분이 없음**: 우리 화면(가시성 개요 등)에서 미수집
  상태와 "수집했지만 0건"이 시각적으로 구분되는지 확인 필요. 지금은
  둘 다 "0"이나 "미노출"로 같아 보일 가능성이 있음 — 데이터 신뢰도
  해석에 영향을 줄 수 있는 부분.
- **변경 비소급 정책이 문서화돼 있지 않음**: 우리도 실제로는 브랜드
  별칭/기타 브랜드를 수정해도 과거 수집분은 재분석 안 하는 구조일
  가능성이 높은데(analyze() 캐시가 input_hash 기준이라 브랜드 목록이
  바뀌면 실제로는 재계산됨 — VIVI와 반대), 이 동작 자체를 사용자에게
  명시적으로 알려주는 문구가 없음. 우리 쪽은 오히려 "다음 분석부터
  브랜드 변경이 즉시 반영된다"(brandSeeds.ts 주석)는 게 설계 의도라
  VIVI와 정책 방향이 다름 — 어느 쪽이 맞는지는 별개로, 이 정책을
  사용자에게 명시하는 문서가 우리한텐 없다는 게 관찰 포인트.
- **Prompt 세트 변경 시 추이 그래프에 불연속 마커가 없음**: 우리
  차트들은 프롬프트 라이브러리가 바뀌어도 아무 표시 없이 쭉 이어진
  선으로 보여줌 — 해석 오류 유발 가능. VIVI의 "Prompt changes" 세로선
  같은 장치가 참고할 만함.
- **변경 사항 스테이징(Modify Prompts의 Apply 방식)이 없음**: 우리는
  프롬프트 라이브러리에서 추가/삭제가 즉시 반영(applyListChange가
  바로 저장). 여러 건을 모아 미리보기 후 한 번에 적용하는 배치形 UX는
  우리한텐 없음 — 실수 방지 관점에서 참고할 만하나, 우리는 지금 항목당
  개별 액션이 즉시 토스트로 확인되는 구조라 트레이드오프가 있음.

---

## 2026-09-23 — VIVI Insight 화면군 상세 (Metrics/Topics/Topic Research/Pages/Prompt Details, 스크린샷 11장 + 문서 5편)

### 원문 요약
- **지표 정의(정밀)**: Mention Rate=자사표기 문자열이 등장한 응답/전체
  응답. Citation Rate=답변이 근거로 든 URL 도메인이 자사 도메인과
  일치한 응답/전체 응답(한 응답이 자사 페이지를 여러 개 인용해도
  분자는 1 — 인용 "건수"가 아니라 인용된 "응답 수"). Share of Voice=
  자사 언급/(자사+Competitor 언급) — 분모가 전체 응답이 아니라
  자사+경쟁사 언급 합이라는 게 다른 세 지표와 결정적으로 다른 점,
  그래서 **Competitor 목록을 나중에 바꾸면 과거 기간의 SoV "표시값"도
  같이 바뀜**(원본 응답 재수집은 안 하지만 분모 재계산은 실시간).
  AI Existence=그 시점에 AI가 답변을 실제로 생성했는지만 보는 지표(내용
  무관), ChatGPT/Gemini/Perplexity는 거의 안 내려가고 Google AI
  Overview·NAVER AI 브리핑처럼 검색결과 안에 조건부로 뜨는 엔진만
  구조적으로 낮아짐.
- **Responses(보조지표)**: 모든 비율의 분모. "질문 50개 × 엔진 3개 =
  응답 150건" 식으로 명시. 응답 수가 작으면 비율이 한두 건 차이로
  크게 흔들린다고 경고.
- **Monthly searches**: Topic=대표 키워드 1개, 그 키워드의 월간
  검색량만 표시(동의어로 묶인 표기의 검색량은 합산 안 함). 출처가
  시장별로 다름 — 한국은 네이버 검색광고 PC+모바일 합산, 그 외
  시장은 Google Ads. 네이버가 못 읽은 키워드는 Google 값으로 대체(한
  국가 안에서도 키워드마다 출처가 섞일 수 있음).
- **Metrics 화면**: Mention/Citation/Compare 3탭. "Exclude brand
  prompts" 토글(질문 자체에 브랜드명이 들어간 것 제외). 상단 카드+
  추이 그래프(Topic별 다중 선)+"Prompt changes" 세로선(질문 추가/수정/
  삭제 시점 표시)+Breakdowns(Topic·Competitor·AI Engine별 분해).
  Share of Voice는 도넛+순위(#3 Rank 식)+경쟁사별 비중 리스트로 별도
  카드. **Category 지정 여부에 따라 축 이름이 동적으로 바뀜** — Category를
  하나도 안 만들면 Topic이 최상위 축, 하나라도 만들면 그 시점부터
  Category가 최상위 축으로 승격되고 화면 라벨도 "Topic"에서
  "Category"로 자동 전환(예전 방식 세팅 프로젝트는 항상 Category로
  고정 표시). Compare 탭: 두 기간(A/B, 길이 달라도 됨) 나란히 비교,
  Δ 표, "모수를 함께 보십시오" 경고(질문/엔진을 늘린 기간은 Responses
  자체가 커져서 비율 변화가 성과가 아니라 측정 범위 확대 때문일 수
  있음).
- **Topics 화면**: Topic(=검색키워드 1개, 측정 여부 결정 단위) > Prompt
  2단계. **상위 단위 지표는 단순평균이 아니라 응답 수 가중 합산**(응답
  많은 질문이 더 크게 반영) — Category·Project 단위도 동일 규칙. 테이블에
  Topic별 AI existence/Mention/Citation/Share of voice/Trend(스파크라인)
  /Status(Measured) 컬럼. 행을 펼치면 그 Topic의 질문 목록 + buyer
  stage 배지(Needs/Research/Compare/Decide/AI Recommend — 질문
  세트가 5단계를 다 덮도록 의도적으로 생성됨, 해당 없으면 하이픈) +
  언급/인용한 자사·Competitor 로고. 질문 수정은 오직 "Modify
  Prompts" 화면 하나로만 — 펼친 목록에서는 못 고침(단일 진입점 강제).
  Delete topic은 측정 중단뿐 아니라 **이후 추천 목록에도 다시 안
  오르게** 만듦(단순 숨김이 아니라 영구 배제).
- **Topic Research 화면**: 아직 안 재는 Topic 후보를 찾는 별도
  탐색 화면(관리는 Topics에서). 키워드 최대 6개 입력(비우면 이미
  측정 중인 Topic에서 확장), 입력한 단어 자체는 후보로 강제 포함되고
  "검색 수요 기준"·"표현 길이 기준"을 면제받음(직접 지정한 시장이
  임의로 걸러지지 않게). 결과 3분류: 신규 발견/이미 발굴됨(중복
  제거)/후보가 걸러짐(사유 명시: 이미 보유·수요 기준 미달·표현
  과다·브랜드나 경쟁사 이름·주제 이탈). 브랜드 제품 영역 밖으로 판정된
  Topic은 기본 숨김, "N개 더 보기" 링크로 사유와 함께 펼칠 수 있음.
  **Simulate 결과가 "board"에 최신순으로 누적 저장**(탐색 결과 목록
  자체는 새로고침하면 사라지지만 이 board는 남음) — 시뮬레이션에서
  고친 질문 목록도 함께 저장. 이미 측정 중인 Topic을 View로 열어
  수정하면 그 변경은 Modify Prompts의 초안(draft)으로 들어감.
- **Pages 화면**: 도메인 기준으로 화면이 갈림 — Official
  Website(자사 대표 도메인, 자동 수집+기술 진단+GSC 연동 지표) vs
  External(자사 도메인 아닌 리테일러/미디어/블로그 인용, 수동 등록).
  카드: Cited Pages(%), Citing Prompts per Page(인용 페이지당 평균
  질문 수), Cited Keywords, Est. MSV of Cited Keywords. **기술 진단
  10개 체크가 2그룹으로 나뉨**: AI-accessible(로드성공/robots
  허용/indexable/서버렌더링 — 4개 **전부** 통과해야 "접근 가능"으로
  카운트, AND 조건) vs SEO basics(title/meta description/canonical/
  구조화 데이터/H1/lang — 6개 중 **평균 통과 개수**로 표시, 통과율이
  아니라 개수라 단위 자체가 다름 — 가장 실패 많은 항목을 "weakest"로
  콜아웃). **Conversion Funnel**: 노출→클릭→세션→전환→매출 5단계,
  각 단계 아래 그 중 "AI 경유" 몫만 따로 표기, 전환/매출은 GA 연동
  시에만 값이 참(Google 생성형 AI 노출은 API 미제공이라 "TBU"로
  표시). 등록(Register)과 핀(Pin)은 별개 동작 — 등록해야 인용 0이어도
  목록에 남고 상세화면 생김, 핀은 그냥 "즐겨찾기" 필터용, 등록 해제해도
  과거 인용 기록은 안 지워짐. 히트맵 뷰(주간 인용 횟수 농도)와
  리스트 뷰 전환 가능.
- **Prompt Details 화면**: 질문 하나의 **4단계 중첩 퍼널** — Total
  Responses ⊇ AI Exist Responses ⊇ **Commercial Opportunity
  Responses**(자사+Competitor 아무나 언급된 응답, 자사/경쟁사 구분
  안 함) ⊇ Mentioned Responses(자사만). 어느 구간에서 크게 줄었는지로
  원인과 처방이 갈림 — 예시(100→80→30→6): "80→30 구간"(AI는 답하지만
  브랜드 자체를 안 언급하는 개념설명형 질문)은 **콘텐츠로 못 고치는
  구간**이라 질문 세트 자체를 조정해야 하고, "30→6 구간"(브랜드는
  언급되는데 자사만 빠짐)은 **콘텐츠 문제**라 Page Optimizer 대상 —
  같은 낮은 Mention Rate라도 원인 진단이 완전히 다름. Commercial
  Opportunity 칸도 등록한 Competitor 범위에 좌우됨(안 등록한 브랜드가
  나열되면 그 응답은 0으로 잡혀 "언급 자체가 없는 질문"처럼 보일 수
  있음). Search Demand는 질문 단위로 따로 안 재고 소속 Topic 값을
  그대로 승계. Cited content types(콘텐츠 유형별 인용 비중, 미분류
  제외)에서 비중 높은 유형에 자사 인용이 0건이면 "콘텐츠 인용 기회"
  콜아웃 표시. 답변에 인용된 영상은 플레이어로 바로 표시(가로/세로
  Shorts 구분).

### 관찰 (우리 제품과 비교)
- **"Visibility Funnel" 아이디어가 이번 회차로 구체화됨**: 지난
  로드맵 메모(visibility-funnel-chart)에 적어둔 아이디어가 VIVI에서는
  Prompt Details의 4단계 중첩 퍼널(전체→AI존재→상업적기회→자사언급)로
  실제 구현돼 있고, 무엇보다 **각 구간별로 처방이 다르다**는 게
  핵심(질문 세트 문제 vs 콘텐츠 문제). 우리도 "미노출" 토픽을 프롬프트
  세트 문제/콘텐츠 문제로 구분 못 하고 있어서, 이 4단계 모델을 그대로
  참고할 만함 — 특히 우리가 이미 다루는 Google AI Overview는 "AI
  Existence가 구조적으로 낮은 엔진"이라 이 구분이 실질적으로 중요.
- **Share of Voice의 "표시값 재계산" 정책이 우리와 다름**: 우리는
  분석 캐시가 input_hash 기준이라 경쟁사 목록을 바꾸면 실제 재분석이
  일어나는 구조. VIVI는 과거 응답 자체는 그대로 두고 SoV "분모 계산"만
  실시간으로 다시 하는 방식 — 정책 차이를 인지하고 있어야 나중에
  "왜 지난달 숫자가 바뀌어 보이지?" 문의에 답할 수 있음.
- **Topic/Category 단위 집계가 가중 평균인지 확인 필요**: VIVI는
  명시적으로 "응답 수 가중" — 우리 가시성 개요/토픽 테이블의 상위 단위
  숫자가 단순 평균인지 가중 평균인지 코드 확인 필요(리서치 노트,
  기획 시 체크리스트 항목으로).
- **buyer stage(구매 여정 5단계) 태깅이 우리한텐 없음**: 우리는
  프롬프트에 search_intent 필드는 있지만 "이 5단계를 다 덮도록
  의도적으로 생성"하는 로직은 없음. 프롬프트 전략/라이브러리 품질
  기준으로 참고할 만함.
- **기술 진단을 AND조건 그룹과 평균카운트 그룹으로 나누는 방식이
  없음**: 우리 사이트맵 크롤은 콘텐츠 가시성(렌더링 비율) 단일
  숫자 위주. "접근 가능 여부(전부 통과해야 함)"와 "SEO 기본기(개수
  평균)"를 분리해서 보여주는 방식은 우리 크롤 결과 화면 설계에
  참고할 만함 — 특히 우리도 구조화 데이터를 체크 항목에 추가하기로
  했으니(이전 리서치 항목) 이 분류 방식에 자연스럽게 들어갈 수 있음.
- **Conversion Funnel(노출→매출)에서 "AI 경유 몫"을 각 단계마다
  따로 표기하는 방식**: 우리는 트래픽 추이가 아직 통째로 mock이라
  단계별 분해 자체가 없음. CDN/Analytics 연동 로드맵 항목에 참고
  가치.
- **Pages의 등록/핀 분리, 인용 0이어도 남는 상세화면**: 우리 URL
  인스펙터는 인용 있는 URL만 목록에 뜨는 구조에 가까움 — "아직 인용
  안 됐지만 추적하고 싶은 URL"을 미리 등록해두는 개념이 없어서, 신규
  발행 콘텐츠를 미리 등록해두고 인용되길 기다리는 워크플로우를 지원
  못 함. 참고할 만함.
- **Topic Research의 "걸러진 후보 사유 명시"**: 단순히 후보를 숨기는
  게 아니라 왜 걸렀는지(수요 미달/표현 과다/브랜드명 등)를 사용자에게
  투명하게 보여줌 — 우리 프롬프트 전략의 "확인 필요"/"콘텐츠 URL
  미입력" 같은 상태 라벨도 이 방향으로 더 구체화할 여지가 있음.

---

## 2026-09-23 — 이미지 재점검: UI/인터랙션 디자인 디테일 (스크린샷 20장 전수 재열람)

앞선 항목들은 텍스트 설명 위주로 기록해서, 스크린샷 20장을 다시 하나씩
열어 화면 캡처만으로 확인되는 시각적/인터랙션 패턴을 별도로 정리한다.
기능 자체(무엇을 측정하는지)보다 **어떻게 보여주는지**에 집중.

### 관찰
- **Funnel을 곡선 그라데이션 밴드로 시각화**: Prompt Details의 4단계
  퍼널이 막대그래프가 아니라, 왼쪽에서 오른쪽으로 갈수록 부드럽게
  좁아지는 보라색 그라데이션 곡선 밴드(마치 물결/깔때기 실루엣)로
  그려짐. 위에 숫자 카드 4개, 아래에 이 곡선 하나 — 단계별 이탈이
  "형태"로 바로 느껴짐. 우리가 퍼널을 실제로 만들 때 단순 막대보다
  이 형태를 검토할 만함.
- **사이드바가 "Insight(측정)"/"Action(실행)" 두 그룹으로 명확히
  분리**: Metrics/Topics/Topic Research/Pages는 Insight 아래,
  Page Optimizer/PDP Convert/Localizer는 Action 아래로 시각적으로
  묶여 있음 — "측정→실행→재측정" 3단계 루프 중 측정과 실행이 메뉴
  구조 자체에서부터 구분됨. 우리 사이드바는 "AI 가시성"과 "기회"가
  개념적으로는 비슷한 역할(측정 vs 개선)인데 지금 그룹 이름이나
  배치가 이 구분을 강하게 드러내지 않음 — 참고할 만한 정보 구조.
- **"준비 중/미래 데이터"를 회색 "TBU" 배지로 명시**: Google이 생성형
  AI 노출수를 API로 아직 안 준다는 이유로 Conversion Funnel의
  Impressions 카드에 숫자 대신 회색 "TBU"를 표시하고 옆에 정보
  아이콘(ⓘ)으로 이유를 설명. 0이나 대시(-)가 아니라 "곧 나올 데이터"
  전용 표기를 따로 둠 — 우리도 "준비 중" 기능과 "값이 0인 실측"과
  "아직 수집 안 함"을 시각적으로 더 구분해도 좋을 듯(VIVI의 0%/하이픈
  구분과 같은 맥락).
- **Conversion Funnel 단계 카드마다 보라색 소문자 보조 숫자
  ("AI 15", "AI 2", "AI $0")**: 메인 숫자(세션/전환/매출) 바로
  아래에 그중 AI 경유분만 작은 보라 텍스트로 병기 — 별도 차트 없이
  "전체 중 AI 몫"을 한 줄로 보여주는 저비용 패턴.
- **기술 진단을 "전부 통과해야 하는 것"과 "개수로 세는 것"을 시각적으로
  구분**: Pages의 AI-accessible(4개, 보라색 진행바, "All four must
  pass" 문구 명시)과 SEO basics(6개, "Independent checks — a page
  can pass any subset" 명시)를 나란히 2컬럼으로 배치하고 각 항목을
  얇은 보라색 프로그레스 바 + 퍼센트로 표시. "Hide/Show all checks"
  토글로 기본은 접어둠(요약 수치만 먼저 보여주고 원하면 펼침).
  약점 항목("weakest: Structured data 69%")을 굵게 콜아웃.
- **List/Heatmap/Pinned only 뷰 전환을 세그먼트 버튼으로**: Pages
  하단 목록에서 같은 데이터를 표/히트맵/즐겨찾기만으로 전환하는 버튼이
  한 줄에 나란히 배치. 우리 URL 인스펙터·수집 실행 목록 같은 긴
  테이블에 참고할 만한 뷰 전환 패턴.
- **행 확장(chevron)형 상세 보기**: Topics 표와 Pages 표 모두 왼쪽
  화살표(›)를 누르면 그 행 아래로 상세 내용이 펼쳐지는 방식(별도
  페이지 이동 없이). Topics를 펼치면 그 안에 buyer stage 배지, 언급/
  인용 여부를 나타내는 작은 원형 브랜드 로고 아이콘들이 프롬프트별로
  붙음 — 텍스트 배지보다 로고 아이콘이 "누가 언급됐는지"를 더 빠르게
  훑을 수 있게 함.
- **Topics 표의 Trend 컬럼이 미니 스파크라인**: 별도 차트 화면 없이
  표 안 좁은 셀 하나에 작은 선 그래프로 최근 추이를 보여줌 — 토픽이
  많아져도 한눈에 스캔 가능. 우리 토픽 테이블에도 숫자 컬럼 옆에
  이런 스파크라인 컬럼을 추가하면 가독성이 오를 수 있음.
- **Simulate 모달의 좌/우 분할 레이아웃**: 왼쪽은 편집 가능한 질문
  리스트(연필/휴지통 아이콘, 하단에 "Add your own prompt" 입력 +
  "Generate with AI" 지시문·개수·버튼 한 줄), 오른쪽은 고정된 보라색
  옅은 배경의 "AI answer prediction" 카드(태그 칩 + 예측 문장 +
  "If you add it:" 불릿) — 왼쪽에서 뭘 고치든 오른쪽 예측 카드는
  그대로 붙어있어 "이렇게 바꾸면 이런 답이 나올 것" 맥락이 안 끊김.
  우리 LLM 브릿지 모달(복사→붙여넣기 2단 레이아웃)과 유사한 "좌:
  입력, 우: 결과/예측" 구조라 참고하기 좋음.
- **Modify Prompts의 상태 필터 탭 + 그룹 헤더**: 전체/Pending/New/
  Edited/Removed/Paused를 탭처럼 가로로 나열(각 옆에 괄호 숫자),
  변경 예정 항목은 Topic 단위로 그룹핑돼서 그룹 헤더에 "Add topic"
  같은 배지와 "Cancel" 버튼이 붙음. 각 질문 행엔 "New" 상태 배지 +
  intent 배지(needs/research/compare/decide/ai_recommend, 색깔
  구분) + 오른쪽 정렬 "Remove" 링크.
- **전체적인 색상 시스템**: 보라 단일 액센트 컬러 하나만 강조색으로
  일관되게 사용(주요 버튼, 활성 탭, 프로그레스 바, 활성 필터 칩),
  옅은 라벤더 배경은 "안내/예측/경고성 콜아웃" 박스 전용, 카드는
  전부 옅은 회색 배경 위 흰 카드+둥근 모서리, 증감 표시만 빨강/초록
  포인트 컬러 사용. 색을 아끼는 절제된 팔레트라 정보 위계가 뚜렷함 —
  우리 UI의 색 사용과 비교해볼 만함(우리도 slate/neutral 위주 + 필요한
  곳만 강조색 쓰는 방향은 비슷하나, VIVI만큼 "액센트 컬러 1개로
  통일"돼 있는지는 재점검 가치 있음).

---

<!--
다음 리서치 항목을 여기 아래에 이어서 추가. 형식은 위 VIVI 항목과 동일하게
"### 원문 요약" / "### 관찰" 두 섹션으로.
-->
